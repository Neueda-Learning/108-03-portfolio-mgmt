spring.application.name=backend

# Run SQL init scripts for non-embedded DBs like MySQL
spring.sql.init.mode=always
spring.sql.init.continue-on-error=false
spring.datasource.url=${URL}
spring.datasource.username=${USERNAME}
spring.datasource.password=${PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always

price.api.base.url=https://c4rm9elh30.execute-api.us-east-1.amazonaws.com/default

finnhub.api.base.url=https://finnhub.io/api/v1
finnhub.api.key=d9olq1hr01qmf0tb776gd9olq1hr01qmf0tb7770

# Local LLM (Ollama)
llm.provider=ollama
ollama.base.url=${OLLAMA_BASE_URL:http://localhost:11434}
ollama.model=${OLLAMA_MODEL:qwen2.5:0.5b}

